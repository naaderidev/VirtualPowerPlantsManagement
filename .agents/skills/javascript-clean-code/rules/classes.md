# Class Rules

Classes are optional.

Use a class when it provides meaningful:
- encapsulated state
- lifecycle
- invariants
- domain behavior
- polymorphism
- resource ownership

Avoid:
- god classes
- deep inheritance trees
- classes with one trivial method
- classes that exist only to imitate another language

Prefer composition when it produces simpler dependencies and clearer behavior.
