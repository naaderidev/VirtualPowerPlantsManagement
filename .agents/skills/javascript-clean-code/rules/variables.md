# Variable Rules

- Prefer `const`; use `let` only when reassignment is required.
- Minimize mutation and mutable shared state.
- Keep variables close to their use.
- Avoid magic numbers and strings; name meaningful domain constants.
- Use narrow scopes.
- Avoid variables that merely rename another value without improving meaning.
- Prefer immutable transformations when they improve safety and readability.
